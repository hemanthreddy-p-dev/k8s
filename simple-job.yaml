apiVersion: batch/v1
kind: Job
metadata:
  name: simple-job
spec:
  backoffLimit: 2
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: job-container
          image: busybox
          command: ["sh", "-c", "echo Hello from Job && sleep 5"]
